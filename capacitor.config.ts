import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lxmusic.controller',
  appName: 'LX Music API Controller',
  webDir: 'dist'
};

export default config;
